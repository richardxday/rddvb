#!/bin/sh
DST="$1"
BACKUP="/home/richard/VideoBackup/${DST%.*}.mpg"
CRF="$2"
test -z "$CRF" && CRF=18
tee "$BACKUP" | avconv -i - -acodec mp3 -vcodec libx264 -crf $CRF -preset veryfast -v warning -y "$DST"
